# v50 — 2026-09-06 C-200 Daily Report operational changes (user-approved "I approve"; caps-only scope confirmed via clarify)

Source: copybot-c200-daily-report (job 84e37616f39a, 2026-09-06 18:00 CDT). Sample N=1,176 finished BANKROLL_200 trades.

## The report's structural case (accepted)

$500/day is +45% of the bankroll. With `maxGrossExposureUsd: 400` pinning the
book at $401–408 and `kellyMaxSizeUsd: 60`, the best sampled day was +$206.78
(Aug 29) — a 7-day $500 streak is physically impossible under the v49 caps.
Two structural levers fix that; both are pure paper risk (downside simulated)
and neither touches the phase gate (still 7 consecutive ≥$500 realized days).

## Implemented — RuleSet v50 (`hermes-v50-c200-report`, activated 2026-09-06)

| field | v49 | v50 |
|---|---|---|
| maxGrossExposureUsd | 400 | **1,000** (equity-link base; NW $1,651 < $1,900 principal → effective cap = base until the hole repays) |
| kellyMaxSizeUsd | 60 | **100** (funds ≤0.20 λ̂=−0.91 / 0.20–0.40 λ̂=−0.21 bands harder) |
| dailyLossLimitUsd | −150 | **−300** (guardrail scaled with the book) |

Verified: RuleSet v50 active, audit row written, 115/115 tests, tsc clean.
Also un-starves the Kelly window (0 [KELLY] lines in v49's first 5h — main-lane
admits were exposure-vetoed at the $400 pin) before the pre-registered window
opens Sep 8.

## Explicitly NOT changed — diffed against live v49 architecture (report agent wrote recs from calibration tables without reading the code; user chose caps-only after the diff was presented)

1. **Band-tilt factors** (`deadZoneSizeFactor`/`longshotSizeFactor`/`deadZoneMinPrice`):
   the report claimed "both size factors set to 1.0 — i.e., no tilt at all."
   Live truth: the tilt exists at the layer that matters — Kelly sizes C-200
   main-lane copies from the band λ̂ table (Sep-1 fit): <0.20 λ̂=−0.91 → funded
   hard; 0.20–0.40 λ̂=−0.21 → sized; **0.40–0.60 λ̂=+0.03 → f*<0 → skipped**;
   ≥0.60 λ̂=+0.21/0.37 → skipped. The rules-layer factors were neutralized at
   1.0 since v39 (paper.ts `mapBankroll200Size` owns the legacy tilt; Kelly
   bypasses both — re-enabling them would double-count the legacy C-200 path
   to ×0.25 in 0.40–0.60 and halve STANDARD-book sizes on C-200-only evidence).
2. **`premiumOverlayEnabled` 0→1**: no-op under Kelly — main-lane Kelly copies
   skip the overlay by design (`!kellySized` guard); Kelly IS the λ̂ mechanism
   (v49 set the overlay to 0 in the same change).
3. **Hour soft-gates (10:00/20:00 ET)**: already live — 10:00 ET has the v41
   50% C-200 haircut; 20:00 ET has the v44/v48 hard blackout. The report's
   hourly drains include pre-gate history in its own sample.

## Standing (unchanged, per earlier approvals)

- c200MaxEntryPrice stays 0.80 (v47) — the 0.80→0.60 candidate stays a
  Sep-15-refit input (#16 rec 3), not a now-change.
- v48 longshot admission lane, v44/v45/v47 STANDARD gates, cadence: unchanged.
- Kalshi: routing eligibility auto-recovered post-reprice; Phase C parked to
  Oct 8 read.
- Sep-15 λ̂ refit will re-fit the band table on the honest (venue-filtered)
  N — the same calibration this report's band claims derive from.
